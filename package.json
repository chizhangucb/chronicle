{
  "name": "chronicle",
  "version": "0.1.8",
  "description": "Local-first AI coding session time machine",
  "main": "electron/main.mjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "desktop": "npm run build && electron .",
    "standalone": "npm run build && node server/standalone.js",
    "dist:mac": "npm run build && electron-builder --mac --arm64 --x64 --publish never",
    "reinstall:mac": "pkill -f 'Chronicle.app/Contents/MacOS/Chronicle' || true; npm run build && electron-builder --mac --arm64 --publish never && ditto release/mac-arm64/Chronicle.app /Applications/Chronicle.app && rm -rf release && open -a /Applications/Chronicle.app",
    "dist:win": "npm run build && electron-builder --win --x64 --publish never",
    "dist:linux": "npm run build && electron-builder --linux --x64 --publish never"
  },
  "keywords": [],
  "author": "Chi Zhang",
  "license": "MIT",
  "build": {
    "appId": "com.chizhang.chronicle",
    "productName": "Chronicle",
    "afterSign": "build/notarize.cjs",
    "publish": {
      "provider": "github",
      "owner": "chizhangucb",
      "repo": "homebrew-chronicle"
    },
    "asar": false,
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**",
      "server/**",
      "electron/**",
      "hooks/**",
      "package.json"
    ],
    "electronLanguages": [
      "en",
      "zh_CN"
    ],
    "mac": {
      "category": "public.app-category.developer-tools",
      "target": [
        "dmg",
        "zip"
      ],
      "hardenedRuntime": true,
      "gatekeeperAssess": false,
      "entitlements": "build/entitlements.mac.plist",
      "entitlementsInherit": "build/entitlements.mac.plist",
      "artifactName": "${productName}-${version}-${arch}.${ext}"
    },
    "dmg": {
      "artifactName": "${productName}-${version}-${arch}.${ext}",
      "format": "ULFO"
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "${productName}-Setup-${version}-${arch}.${ext}"
    },
    "linux": {
      "target": [
        "AppImage",
        "deb"
      ],
      "category": "Development",
      "maintainer": "Chi Zhang <chi.zhang@gokite.ai>",
      "artifactName": "${productName}-${version}-${arch}.${ext}"
    }
  },
  "type": "module",
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.3",
    "diff": "^9.0.0",
    "@electron/notarize": "^2.5.0",
    "electron": "^43.0.0",
    "electron-builder": "^26.15.3",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "vite": "^8.1.3"
  },
  "dependencies": {
    "electron-updater": "^6.3.9",
    "express": "^5.2.1"
  }
}
